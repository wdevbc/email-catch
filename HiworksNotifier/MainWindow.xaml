<Window x:Class="HiworksNotifier.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HiworksNotifier"
        mc:Ignorable="d"
        Title="Hiworks Note" Width="340" SizeToContent="Height"
        Topmost="True" WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ShowInTaskbar="False" ResizeMode="NoResize"
        WindowStartupLocation="Manual">

    <Border Background="#FFFFFF" Margin="10" CornerRadius="8" Padding="0" 
            MouseLeftButtonDown="Window_MouseLeftButtonDown"
            MouseLeftButtonUp="Window_MouseLeftButtonUp"
            MouseMove="Window_MouseMove">
        
        <Border.Effect>
            <!-- Soft modern shadow -->
            <DropShadowEffect BlurRadius="15" ShadowDepth="4" Direction="270" Color="#222" Opacity="0.15"/>
        </Border.Effect>
        
        <!-- Entry Animation -->
        <Border.RenderTransform>
            <TranslateTransform/>
        </Border.RenderTransform>
        <Border.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4"/>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="20" To="0" Duration="0:0:0.4">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>

                        <!-- Shake Effect (Starts after entry) -->
                        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)" BeginTime="0:0:0.4" RepeatBehavior="3x">
                            <EasingDoubleKeyFrame KeyTime="0:0:0.05" Value="-5"/>
                            <EasingDoubleKeyFrame KeyTime="0:0:0.10" Value="5"/>
                            <EasingDoubleKeyFrame KeyTime="0:0:0.15" Value="-5"/>
                            <EasingDoubleKeyFrame KeyTime="0:0:0.20" Value="5"/>
                            <EasingDoubleKeyFrame KeyTime="0:0:0.25" Value="-3"/>
                            <EasingDoubleKeyFrame KeyTime="0:0:0.30" Value="3"/>
                            <EasingDoubleKeyFrame KeyTime="0:0:0.35" Value="0"/>
                        </DoubleAnimationUsingKeyFrames>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Border.Triggers>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/> <!-- Accent Strip -->
                <ColumnDefinition Width="*"/>    <!-- Content -->
                <ColumnDefinition Width="Auto"/> <!-- Close Button -->
            </Grid.ColumnDefinitions>

            <!-- Blue Accent Strip (Hiworks Brand Color) -->
            <Border Grid.Column="0" Width="4" Background="#0078D7" CornerRadius="8,0,0,8" Margin="0,0,12,0"/>

            <!-- Content -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="0,12,0,12">
                <TextBlock Text="{Binding Sender}" FontFamily="Segoe UI Semibold" FontSize="14" Foreground="#1F1F1F" TextTrimming="CharacterEllipsis"/>
                <TextBlock Text="{Binding Subject}" FontFamily="Segoe UI" FontSize="13" Foreground="#4A4A4A" TextTrimming="CharacterEllipsis" Margin="0,4,0,0" MaxHeight="36"/>
                <TextBlock Text="{Binding Date, StringFormat='tt h:mm'}" FontFamily="Segoe UI" FontSize="11" Foreground="#999" Margin="0,6,0,0"/>
            </StackPanel>

            <!-- Close Button -->
            <Button Grid.Column="2" Click="CloseButton_Click"
                    Width="24" Height="24" Margin="8,8,8,0" VerticalAlignment="Top"
                    Background="Transparent" BorderThickness="0" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Grid Background="{TemplateBinding Background}">
                            <Path Data="M0,0 L10,10 M10,0 L0,10" Stroke="#999" StrokeThickness="1.5" 
                                    HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="0.5,0.5">
                                <Path.RenderTransform>
                                    <ScaleTransform ScaleX="0.8" ScaleY="0.8"/>
                                </Path.RenderTransform>
                            </Path>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F2F2F2"/> <!-- Subtle hover bg -->
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="12"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>
    </Border>
</Window>
